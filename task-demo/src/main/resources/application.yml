server:
  port: 8006
spring:
  application:
    name: task-demo
  redis:
    host: ${ENV_CLOUD_IP}
    port: 6379
    password: chenqfredis
  mail:
    password: ${ENV_MAIL_USERNAME}
    username: ${ENV_MAIL_PASSWORD}
    host: smtp.qq.com
    port: 465
    properties:
      mail:
        smtp:
          ssl:
            enable: true
